openapi: 3.0.1
info:
  title: ObjectStorageAPI
  description: API Definition of ObjectStorageAPI
  version: 1.0.0
servers:
- url: http://localhost:8290/object
paths:
  /gcs/{operation}:
    post:
      parameters:
      - name: operation
        in: path
        required: true
        schema:
          type: string
      requestBody:
        description: Sample Payload
        content:
          application/json:
            schema:
              type: object
              properties:
                payload:
                  type: object
          application/xml:
            schema:
              type: object
              xml:
                name: payload
        required: false
      responses:
        default:
          description: Default response
